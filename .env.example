# Production domain. Use a DNS name for automatic HTTPS with Caddy/Let's Encrypt.
DOMAIN=scheduler.example.com

# IP-only or private HTTP deployment examples:
# DOMAIN=:80
# DOMAIN=http://192.0.2.10

NODE_ENV=production
